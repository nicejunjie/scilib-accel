

#define GiB 1024*1024*1024;
#define MiB 1024*1024;
#define KiB 1024;

// Define the DEBUG macro
#define DEBUG1(x) do { if (debug==1) { x; } } while (0)
#define DEBUG2(x) do { if (debug==2) { x; } } while (0)


